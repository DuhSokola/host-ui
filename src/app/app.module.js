;(function() {

    'use strict';

    var dependencies = [
        'routeConfig'
    ];

    var app = angular.module('hostApp', dependencies);

    app.run(function () {

    });

    app.config(function () {

    });

}());
