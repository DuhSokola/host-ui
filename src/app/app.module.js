;(function() {

    'use strict';

    var dependencies = [
        'ngRoute'
    ];

    var app = angular.module('hostApp', dependencies);

    app.run(function () {

    });

    app.config(function () {

    });

}());
