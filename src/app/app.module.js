;(function() {

    'use strict';

    var dependencies = [
        'pascalprecht.translate',
        'routeConfig'
    ];

    var app = angular.module('hostApp', dependencies);

    app.run(function () {

    });

    app.config(function () {

    });

}());
