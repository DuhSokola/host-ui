;(function(){
    'use strict';

    var dependencies = [
        'ngRoute'
    ];

    var route = angular.module('hostApp.routes',dependencies);

    route.config(function($routeProvider){

    });

}());