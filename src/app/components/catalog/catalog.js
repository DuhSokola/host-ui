;(function(){
    'use strict';

    var deps = [
        'hostApp.catalog.ctrl',
        'hostApp.catalog.directives'
    ];

    var catalog = angular.module('hostApp.catalog',deps);

    catalog.config(function(){

    });

    catalog.run(function(){

    });

}());