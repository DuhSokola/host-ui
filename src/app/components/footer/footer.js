;(function(){
    'use strict';

    var deps = [
        'hostApp.footer.ctrl',
        'hostApp.footer.directives'
    ];

    var footer = angular.module('hostApp.footer',deps);

    footer.config(function(){

    });

    footer.run(function(){

    });

}());