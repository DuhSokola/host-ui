;(function(){
    'use strict';

    var deps = [
        'hostApp.header.ctrl',
        'hostApp.header.directives'
    ];

    var header = angular.module('hostApp.header',deps);

    header.config(function(){

    });

    header.run(function(){

    });

}());