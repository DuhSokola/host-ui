;(function(){
    'use strict';

    var deps = [
        'hostApp.search.ctrl',
        'hostApp.search.directives'
    ];

    var search = angular.module('hostApp.search',deps);

    search.config(function(){

    });

    search.run(function(){

    });

}());